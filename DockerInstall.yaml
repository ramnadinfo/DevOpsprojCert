  - hosts: docker
    gather_facts: false
    become: yes
    tasks:
      - name: Install docker
        yum:
          name: docker
          state: started
      - shell: "docker -v"
        register: docker_verInfo
      - debug: var=docker_verInfo
